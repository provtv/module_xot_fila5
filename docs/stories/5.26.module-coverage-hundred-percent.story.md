---
id: STORY-XOT-5.26
epic: 5
story: 26
title: "Coverage 100% per modulo — con mutation score, perché il 100% da solo non significa niente"
scope: module:Xot
owned_scope:
  - laravel/Modules/*/tests
  - laravel/Modules/*/phpunit.xml
  - laravel/Modules/*/docs/testing.md
  - laravel/Modules/Xot/docs/coverage.md
status: ready-for-dev
language: it-IT
created_at: '2026-08-19'
updated_at: '2026-08-19'
tags: [pest, coverage, mutation-testing, xdebug, quality-gates, nwidart, long-run]
repository: "https://github.com/laraxot/module_xot_fila5"
issues: "https://github.com/laraxot/module_xot_fila5/issues/80"
discussions: "https://github.com/laraxot/module_xot_fila5/discussions/17"
related:
  - ./5.24.module-coverage-fifty-percent-floor.story.md
  - ./5.25.module-suite-green-offline.story.md
  - ./5.22.module-coverage-plus-ten-campaign.story.md
  - ./5.2.pest-baseline-coverage.story.md
  - ../coverage.md
  - ../../../../../../docs/chat/coverage-misurabilita-suite-moduli.md
---

# Story 5.26: coverage 100% per modulo

Status: ready-for-dev

## Story

Come **tech lead quality**,
voglio **il 100 % di statement coverage su `Modules/<Nome>/app` di ogni modulo, accompagnato
da un mutation score che ne dimostri il valore**,
così che **nessuna riga di dominio resti mai eseguita per la prima volta in produzione, e il
numero sul badge corrisponda a una suite che troverebbe davvero una regressione**.

## Obiezione, scritta prima di iniziare

Il 100 % **imposto come soglia** è il modo più affidabile di produrre test filler: un test
che chiama un metodo e non asserisce niente alza la percentuale esattamente come un test che
protegge un comportamento. La story [5.24](./5.24.module-coverage-fifty-percent-floor.story.md)
lo vieta già («escluso dal conteggio utile: test filler, assert su `fix:`, duplicazione
helper»), ma un target del 100 % rende quel divieto molto più difficile da rispettare che
un floor del 50 %.

Questa story quindi **non** chiede solo il 100 %: chiede il 100 % **più** un mutation score
minimo sullo stesso perimetro, **più** un perimetro dichiarato onestamente. Le tre cose
insieme sono verificabili; la prima da sola no. `pestphp/pest-plugin-mutate` v5.0.2 è già
installato: lo strumento c'è, va solo puntato.

Se preferisci il 100 % secco senza mutation score, è una tua decisione e la story si esegue
lo stesso — ma allora il numero misura quanto codice viene *attraversato*, non quanto è
*protetto*, e va detto quando lo si cita.

## Punto di partenza misurato — 2026-08-19

Da `Modules/Xot/docs/coverage.md`, calcolato sui clover in `build/coverage-baseline/`:

| Modulo | Coverage | Statement scoperti | Metodi coperti |
|---|---:|---:|---:|
| Ptv | 2,9 % | 6 779 | 34 / 691 |
| Pdnd | 0,0 % | 6 327 | 0 / 1 031 |
| Performance | 5,2 % | 5 455 | 29 / 461 |
| Progressioni | 0,0 % | 5 114 | 1 / 412 |
| Sigma | 0,1 % | 5 060 | 0 / 466 |
| Notify | 15,4 % | 4 315 | 119 / 568 |
| Media | 13,4 % | 2 712 | 59 / 327 |
| IndennitaResponsabilita | 3,1 % | 2 604 | 80 / 322 |
| Job | 10,3 % | 1 875 | 31 / 317 |
| IndennitaCondizioniLavoro | 3,6 % | 1 654 | 33 / 196 |
| UI | 11,4 % | 1 542 | 38 / 247 |
| Lang | 21,3 % | 878 | 18 / 142 |
| Tenant | 34,9 % | 598 | 33 / 158 |
| Activity | 29,0 % | 547 | 42 / 135 |
| Rating | 70,9 % | 181 | 71 / 101 |
| **Totale** | **7,1 %** | **45 641** | **588 / 5 896** |

**45 641 statement da coprire**, e il numero è una **stima per difetto**: `Incentivi`, `User`
e `Xot` non hanno ancora prodotto un clover (rispettivamente: fatal da classe anonima, run non
conclusa, misura non eseguita). `Xot` da solo ha 532 file in `app/`.

Sei moduli — Ptv, Pdnd, Performance, Progressioni, Sigma, Notify — valgono **33 050
statement, il 72 % del totale**. Qualunque piano che non li affronti per primi non sposta
l'ago.

## Acceptance Criteria

1. Per ogni modulo (escluso `TestModule`):
   `XDEBUG_MODE=coverage ./vendor/bin/pest -c Modules/<Mod>/phpunit.xml --coverage --min=100`
   → **exit 0**.
2. Sullo stesso perimetro:
   `./vendor/bin/pest -c Modules/<Mod>/phpunit.xml --mutate --min=<soglia>` → exit 0.
   Soglia iniziale **60**, da alzare per gradi. Un modulo con coverage 100 % e mutation
   score basso ha test che eseguono il codice senza verificarlo: **l'AC non è soddisfatta**.
3. Il perimetro `<source>` di `Modules/<Mod>/phpunit.xml` è **dichiarato, non gonfiato né
   svuotato**: ogni directory esclusa è elencata in `Modules/<Mod>/docs/testing.md` con il
   motivo. Escludere codice per far salire la percentuale, senza motivo scritto, è una
   violazione dell'AC, non una scorciatoia.
4. **Zero `dddx()` in `Modules/*/app`**: oggi sono **357**. `dddx()` è dump-and-die — un test
   che raggiunge quella riga uccide il processo. Non si coprono: si **cancellano**, insieme
   al ramo morto che le contiene.
5. Nessun `@codeCoverageIgnore` aggiunto per far quadrare il numero. Se una riga non è
   raggiungibile da un test, o è morta (si cancella) o il perimetro va corretto (AC 3).
6. Suite verde al momento della misura, `--min=100` compreso; skip solo **condizionati** su
   precondizione d'ambiente (AD-4).
7. `./vendor/bin/phpstan analyse Modules` → `[OK] No errors` a fine di ogni wave: i test
   nuovi sono codice, e il gate statico vale anche per loro.
8. Type coverage globale **non peggiora**: `typeCoverage.*` resta sotto soglia (oggi il
   margine è di ~14 parametri, vedi [5.21](./5.21.type-coverage-headroom.story.md)).
9. `Modules/Xot/docs/coverage.md` aggiornato a ogni wave con coverage **e** mutation score.

## Tasks / Subtasks

- [ ] **T0 — Prerequisiti che non dipendono da questa story (AC: 6)**
  - [ ] [5.25](./5.25.module-suite-green-offline.story.md): suite verdi offline. Finché i
        test falliscono con `no such table`, il codice che avrebbero coperto **non viene
        eseguito**: risolvere lo schema alza la coverage senza scrivere un test in più
  - [ ] Misurare `Incentivi`, `User`, `Xot`: senza i loro clover il totale è ignoto
- [ ] **T1 — Ripulire ciò che non si può coprire (AC: 4, 5)**
  - [ ] Censire i 357 `dddx()` in `Modules/*/app`; per ognuno: la riga è debug residuo
        (cancellare) o un ramo vivo (sostituire con un'eccezione o un log)
  - [ ] Cancellare il dead code già noto: `ZipSchedaHeaderAction` (corpo `dddx('a')`, zero
        riferimenti), i tre RelationManager orfani sotto `Ptv/.../AnagResource/` (la Resource
        non esiste), `HasLikes` se `Modules\Rating\Models\Like` non verrà creato
  - [ ] Dichiarare in `docs/testing.md` di ogni modulo le directory escluse dal `<source>`
- [ ] **T2 — Wave A, i moduli dove il 100 % è raggiungibile (AC: 1, 2, 9)**
  - [ ] Rating (181 scoperti), Activity (547), Tenant (598), Lang (878)
  - [ ] Sono il banco di prova del mutation score: fissare qui la soglia prima di scalare
- [ ] **T3 — Wave B, medi (AC: 1, 2, 9)**
  - [ ] UI (1 542), IndennitaCondizioniLavoro (1 654), Job (1 875),
        IndennitaResponsabilita (2 604), Media (2 712)
- [ ] **T4 — Wave C, i sei che valgono il 72 % (AC: 1, 2, 9)**
  - [ ] Notify (4 315), Sigma (5 060), Progressioni (5 114), Performance (5 455),
        Pdnd (6 327), Ptv (6 779)
  - [ ] Pdnd e Sigma partono da **zero metodi coperti su 1 031 e 466**: qui non si «alza la
        coverage», si scrive da capo la suite di un modulo
- [ ] **T5 — Xot per ultimo (AC: 1, 2, 7)**
  - [ ] È il kernel: ogni test di ogni modulo passa da `XotBaseTestCase`. Un cambiamento
        sbagliato qui non rompe un modulo, li rompe tutti
- [ ] **T6 — Rendere il numero non regredibile (AC: 1, 9)**
  - [ ] `--min=100` per modulo nel gate, così il 100 % raggiunto non può scendere in silenzio

## Dev Notes

### Cosa rende il 100 % diverso dal 50 %, in pratica

Il 50 % si raggiunge coprendo ciò che è facile: Actions pure, Enum, Data, Policy con mock.
Il 100 % obbliga a coprire ciò che di solito si lascia indietro, ed è lì che il target o
produce valore o produce filler:

| Categoria | File | Rischio |
|---|---|---|
| Filament (Resources, Schemas, Tables, Pages) | **1 623 su 3 866** in `app/`, il 42 % | Sono dichiarazioni di schema. Un test che chiama `getTableColumns()` e conta le chiavi copre molte righe e verifica poco: è il territorio naturale del filler. Coprirli **con asserzioni sul contenuto**, non sul numero |
| Service Provider | 63 file | Eseguiti dal boot di Testbench: la coverage arriva quasi gratis, ma verifica poco |
| Rami di errore | — | Sono la parte che il mutation score premia davvero: `--mutate` cambia un `>` in `>=` e vede se un test se ne accorge |

### Il tetto strutturale, ancora aperto

`database/fixcity_data.sqlite` — su cui `XotBaseTestCase` rimappa ogni connessione — contiene
sette tabelle e nessuna di dominio. Ogni test che tocca lo schema fallisce con `no such table`
e **non esegue** il codice che avrebbe coperto. Con `Pdnd` a 1 031 metodi non coperti su
1 031 e `Sigma` a 466 su 466, una parte grande del 100 % è oggi **irraggiungibile per
costruzione**, non per pigrizia.

Le tre vie sono in [5.25](./5.25.module-suite-green-offline.story.md). **Novità utile:** un
MySQL locale ascolta su `127.0.0.1:3306`; le credenziali di `.env.testing` (`personale`) non
vi accedono e nemmeno `root` senza password. Se ne esistono di valide, la via C — l'unica
conforme ad AD-3 — è **una riga in `.env.testing`**. Verificarlo prima di tutto il resto:
cambia la stima di questa story di un ordine di grandezza.

### Trappole di misura, già pagate

1. **Pest stampa la tabella di coverage solo su run con exit 0**, ma **il clover lo scrive
   comunque**. Per questo la baseline esiste anche per moduli con suite rossa — ed è per
   questo che quei numeri **saliranno da soli** quando la suite diventerà verde.
2. **`--coverage-filter` da CLI è accettato e ignorato.** Sposta il perimetro solo il
   `<source>` della `phpunit.xml` del modulo.
3. **Xdebug è l'unico driver** (`pcov` non installato), `XDEBUG_MODE=coverage` obbligatorio.
   Il run è lento: con `--mutate` lo è molto di più, perché rilancia la suite per ogni
   mutante. Misurare un modulo alla volta, mai tutto insieme.
4. **Le classi anonime rompono la misura.** `new class extends X` produce un nome contenente
   un byte NUL (`class@anonymous\0/path.php:riga`); Xot ne deriva il path di un file di lingua
   e `file_put_contents()` solleva `ValueError: must not contain any null bytes`. È il fatal
   che azzera il run di `Incentivi`. **Sottoclasse nominata dentro il file di test**, sempre.
5. **`test('…')->skip()` non compila sotto PHPStan** (`function.void`, `method.nonObject`):
   si usa `todo('motivo')`.

### File in UPDATE da leggere prima di toccarli

- `Modules/Xot/tests/XotBaseTestCase.php` — righe ~148-180 e ~275-330: rimappa ogni
  connessione sqlite e condivide un solo PDO. Kernel di ogni test di ogni modulo. Portare
  Xot al 100 % (T5) significa toccarlo: leggerlo per intero prima.
- `Modules/Xot/app/Filament/Traits/HasXotTable.php` — la rinomina
  `getTableColumns()` → `getXotTableColumns()` ha lasciato bridge legacy che delegano ai nuovi
  nomi. Un test scritto contro il nome sbagliato misura il bridge, non il comportamento.
  Vedi issue [#75](https://github.com/laraxot/module_xot_fila5/issues/75).
- `Modules/Rating/app/Models/Traits/HasLikes.php` — fa `morphMany(Like::class, …)` su una
  classe **che non esiste nel tree**. Coprirlo con un test è coverage che non descrive
  niente: prima si decide se `Like` va creato o il trait cancellato.

### Project Structure Notes

- Story in `Modules/Xot/docs/stories/` perché `scope: module:Xot`; perimetro reale in
  `owned_scope`. Gate: `bashscripts/quality-gates/check-bmad-story-location.sh` (oggi 1 solo
  `FAIL` residuo, su una story di root creata da un'altra sessione).
- Numerazione satura e con duplicati (`5.18`, `5.19`, `5.20`, `5.21` esistono in doppia copia
  con titoli diversi). Prima di creare la prossima:
  `ls Modules/Xot/docs/stories/*.story.md | xargs -n1 basename | sort -V | tail`.

### Testing

- Solo Pest (AD-6); `uses(\Modules\<Mod>\Tests\TestCase::class);` nuda in testa a ogni file.
  Vietate le catene `uses()->in()` e `pest()->extend()`.
- `DatabaseTransactions`, mai `RefreshDatabase` (AD-3).
- Helper condivisi: metodi statici su `Modules\Xot\Tests\XotBasePest`. **Mai** funzioni
  globali, **mai** `tests/Support/` — è la causa dei 23 fallimenti di Media
  (`assertMediaDeclaresStrictTypes()`, `assertMediaUsesQueueableAction()`: non esistono in
  nessun file) e dei 460 `function.notFound` che User si è portato dietro per un giorno.

### References

- [Source: Modules/Xot/docs/coverage.md] — baseline misurata, gap per modulo, trappole
- [Source: Modules/Xot/docs/stories/5.25.module-suite-green-offline.story.md] — le tre vie
  per lo schema; precondizione di questa story
- [Source: Modules/Xot/docs/stories/5.24.module-coverage-fifty-percent-floor.story.md] —
  floor 50 %, strategia ROI, divieto di filler
- [Source: .../ARCHITECTURE-SPINE.md#AD-3,#AD-4,#AD-6,#AD-7,#AD-10,#AD-17]
- [Source: docs/chat/coverage-misurabilita-suite-moduli.md] — misure e diagnosi condivise
- `pestphp/pest-plugin-mutate` v5.0.2 — installato, mai usato finora

## Comandi

```bash
# cwd: laravel/

# coverage del modulo, con la soglia della story
XDEBUG_MODE=coverage ./vendor/bin/pest -c Modules/{Mod}/phpunit.xml --coverage --min=100

# mutation score sullo stesso perimetro (lento: rilancia la suite per ogni mutante)
./vendor/bin/pest -c Modules/{Mod}/phpunit.xml --mutate --min=60

# clover, per ricalcolare la tabella di coverage.md
XDEBUG_MODE=coverage ./vendor/bin/pest -c Modules/{Mod}/phpunit.xml \
  --coverage --coverage-clover build/coverage-baseline/clover-{Mod}.xml --min=0

# percentuali di tutti i moduli dai clover gia' prodotti
php -r '
foreach (glob("build/coverage-baseline/clover-*.xml") as $f) {
    $mod = preg_replace("#^clover-|\.xml$#", "", basename($f));
    $m = @simplexml_load_file($f)->project->metrics ?? null;
    if (!$m) continue;
    $st = (int) $m["statements"]; $cov = (int) $m["coveredstatements"];
    printf("%-28s %6.1f%%  %6d/%-6d\n", $mod, $st ? 100*$cov/$st : 0, $cov, $st);
}'

# censimento dei dddx() da eliminare
grep -rn 'dddx(' Modules/*/app --include=*.php | wc -l
```

## Dev guardrails (LOCKED)

| Regola | Dettaglio | Fonte |
|---|---|---|
| Filler | un test senza asserzione sul comportamento non conta, anche se alza la percentuale | 5.24 |
| Perimetro | ogni esclusione dal `<source>` è motivata per iscritto | AC 3 |
| Soppressioni | nessun `@codeCoverageIgnore` per far quadrare il numero | AC 5 |
| `dddx()` | si cancella, non si copre | AC 4 |
| Neon | `laravel/phpstan.neon` immutabile per gli agenti | AD-1 |
| DB | mai `RefreshDatabase`/`migrate:fresh`/`migrate --force` fuori dal file di test | AD-3 |
| Lock | `bashscripts/lock/*` dalla root repo; staleness dall'ora, mai dal PID | AD-15 |
| Scope | un agente = un modulo per wave; i difetti altrui si segnalano in `docs/chat/` | AD-14 |

## Dev Agent Record

### Agent Model Used

claude-opus-5[1m]

### Debug Log References

- `laravel/build/coverage-baseline/clover-*.xml` — 15 clover, baseline del 2026-08-19
- `laravel/Modules/Xot/docs/coverage.md` — tabella derivata

### Completion Notes List

- 2026-08-19 — story creata sulla baseline reale (7,1 %), non su una stima. 45 641 statement
  scoperti sui 15 moduli misurati, tre moduli ancora senza misura. L'obiezione al 100 % come
  soglia secca è scritta in testa alla story invece di essere lasciata implicita.

### File List
