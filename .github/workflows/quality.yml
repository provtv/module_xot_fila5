name: Xot Quality Checks

on:
  push:
    branches:
      - develop
      - main
    paths:
      - 'laravel/Modules/Xot/**'
  pull_request:
    branches:
      - develop
      - main
    paths:
      - 'laravel/Modules/Xot/**'

jobs:
  quality:
    runs-on: ubuntu-latest
    environment: Testing
    
    steps:
      - uses: actions/checkout@v5

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.4'

      - name: Add Flux Credentials Loaded From ENV
        run: composer config http-basic.composer.fluxui.dev "${{ secrets.FLUX_USERNAME }}" "${{ secrets.FLUX_LICENSE_KEY }}"

      - name: Install Dependencies
        run: |
          cd laravel
          composer install -q --no-ansi --no-interaction --no-scripts --no-progress --prefer-dist

      - name: Run PHPStan
        working-directory: laravel
        run: |
          ./vendor/bin/phpstan analyse Modules/Xot --level=10 --memory-limit=-1 || true

      - name: Run PHPMD
        working-directory: laravel
        run: |
          if [ -f phpmd.phar ]; then
            php phpmd.phar Modules/Xot text cleancode,codesize,design,naming,unusedcode || true
          else
            echo "PHPMD not found, skipping..."
          fi

      - name: Run Pint
        working-directory: laravel
        run: |
          ./vendor/bin/pint Modules/Xot || true

      - name: Run PHPInsights
        working-directory: laravel
        run: |
          if [ -f vendor/bin/phpinsights ]; then
            ./vendor/bin/phpinsights analyse Modules/Xot --format=table --no-interaction --min-quality=70 || true
          else
            echo "PHPInsights not found, skipping..."
          fi
